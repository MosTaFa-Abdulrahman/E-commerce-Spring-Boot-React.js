{
  "buildCommand": "mvn clean package -DskipTests",
  "outputDirectory": "target",
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/api/$1"
    }
  ],
  "env": {
    "JAVA_HOME": "/opt/java/openjdk"
  }
}
